# Examples

Planned examples:

- `backtest`: run the included backtester against a downloaded dataset
- `record`: start Binance and Polymarket collectors in paper-safe mode
- `export-features`: export aligned features from a SQLite snapshot
- `train-xgboost`: train a baseline model from processed features

The first stable public example should run with:

```bash
cargo run --example backtest
```
