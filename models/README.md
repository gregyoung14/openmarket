# Models

Do not commit trained model binaries to Git.

Public model artifacts should be released at:

```text
huggingface.co/gregyoung14/openmarket-models
```

This directory is for lightweight manifests, feature schemas, and notes.
